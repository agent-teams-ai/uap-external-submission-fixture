# submission-e2e

This repository is a non-production fixture for Directory fork-PR E2E. It exists only to verify a disposable contributor submission flow and must not be treated as a useful plugin.

The package contains one harmless, explicitly invoked skill. It has no executable code, hooks, MCP servers, network calls, credentials, dependencies, generated client output, or write behavior.
